@model IEnumerable<E_knjiznica.Models.PODRUZNICA>

@{
    ViewData["Title"] = "Index";
}

<h1>Naše Lokacije</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipPodruznice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Posta)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipPodruznice.Naziv)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Posta)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_podruznice">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_podruznice">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_podruznice">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
